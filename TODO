
 TO DO
 =====

* ability to define maximal file size can be scanned
* file extension whitelist and blacklist (whitelisted files
  are never scanned and blacklisted are scanned even they are
  bigger than file limit)
* negative cache (cache information about infected files)
* better configuration file handling (preferably using POCO)
* ability to set all parameters from command line?
* mount helper?
* daily file system statistics?
